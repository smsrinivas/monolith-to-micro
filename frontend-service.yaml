apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend
  labels:
    app: udagram-frontend
spec:
  type: LoadBalancer
  selector:
    app: udagram-frontend
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  